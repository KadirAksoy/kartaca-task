spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kartacadb  
    username: admin
    password: 1234
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: 'false'
    hibernate:
      ddl-auto: update

kafka:
  brokers: localhost:59092   # kafka:9092 şeklinde değiştirilmeli
server:
  port: 8080
